server:
  port: 8084

spring:
  application:
    name: admission-service
  datasource:
    url: jdbc:postgresql://postgres:5432/esaa_admission
    username: ${ADMISSION_DB_USERNAME:admission_user}
    password: ${ADMISSION_DB_PASSWORD:admission_secure_password_2024}
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true

eureka:
  client:
    service-url:
      defaultZone: http://eureka-server:8761/eureka/
    register-with-eureka: true
    fetch-registry: true
  instance:
    prefer-ip-address: true
    hostname: admission-service

management:
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    health:
      show-details: always

logging:
  level:
    com.esaa.admission: DEBUG 
